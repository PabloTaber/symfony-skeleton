<div class="mt-3">
    {{ form_start(form) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-outline-success me-2">Actualizar</button>
        <a class="btn btn-outline-secondary me-2" href="{{ path('app_user_index') }}" role="button">Volver a la lista</a>
        {{ include('user/_delete_form.html.twig') }}

    {{ form_end(form) }}
</div>